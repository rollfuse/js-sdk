# sdk-js

Placeholder for the JavaScript SDK that will evaluate feature flags locally
against cached, versioned configuration (see ADR 0004: Evaluate Flags
Locally in SDKs).

No implementation exists yet. This package has no reason to exist before
Phase 1 (Feature Management) introduces flags and a configuration
distribution format for SDKs to consume.
